# 媒体查询

## 范例

```css
@media ( condition ) {
    /* 如果符合条件（condition），那么这里的CSS就会被应用 */
}

@media ( max-width: 100px ) {
    /* 如果视口的宽度值属于[0px, 100px]区间，那么<p>就会应用红色字体 */
    p {
        color: red;
    }
}
```


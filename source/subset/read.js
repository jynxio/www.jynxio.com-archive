const fs = require( "fs" );
const path = require( "path" );

/**
 * 读取指定文件或指定目录下的所有文件
 * @param { string } path 文件或文件夹的路径
 */
async function read ( path ) {

	/* TODO */

}

module.exports = { read };
